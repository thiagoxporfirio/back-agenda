import { Router } from 'express';

const routes = Router();

// routes.use('/auth', authRoutes);
// routes.use('/courts', courtRoutes);
// routes.use('/bookings', bookingRoutes);

export default routes;